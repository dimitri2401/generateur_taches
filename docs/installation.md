## Installation

#### Version exécutable

Windows :

- Télécharger l'archive suivante
- La décompresser.

Linux :

- Télécharger l'archive suivante
- La décompresser.

MacOS (exécutable non testé) :

- Télécharger l'archive suivante
- La décompresser.

#### Code source Python

- Cloner le répertoire Git ou le télécharger depuis GitHub.
- Recommandé : créer un environnement virtuel Python dans le répertoire.
- Veiller à installer toutes les dépendances `pip install -r requirements.txt`. Attention, un compilateur C++ doit être configuré sur la machine afin de pouvoir compiler la librairie `highspy`.